services:
  depopbot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/app/data/depopbot.db
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
    volumes:
      - depopbot-data:/app/data

volumes:
  depopbot-data:
